<h2 style='margin:0px'>Login</h2>

<%= form_for user, :url => {:action=>"create", :controller=>"devise/sessions"} do |f| %>
  <% if user.errors.any? %>
		<%= render :partial => 'partials/errors', :locals => {:objects => [user]}%>
	<% end %>
  <p>
		<%= f.label :username %><br />
  	<%= f.text_field :username %>
		<%= render :partial => 'partials/error_message', 
			:locals => {:object => user, :attribute => :username}%>
	</p>
	<p>	
	  <%= f.label :password %><br />
	  <%= f.password_field :password %><br />
		<%= render :partial => 'partials/error_message', 
			:locals => {:object => user, :attribute => :password}%>
	</p>
  <%= f.submit 'Login' %>
<% end %>
