require 'test_helper'

class RingObserverTest < ActiveSupport::TestCase
  #The test for the creation of the public profile is already included in user_observer_test.
  #This is because public profiles are triggered by the creation of the public ring, which is in turn triggered by the creation of the user 
end
